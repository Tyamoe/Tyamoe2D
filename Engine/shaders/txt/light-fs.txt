precision mediump float;

varying vec3 fragmentPosition;
varying vec2 fragmentUV;

uniform vec4 color;

void main() 
{
	float distance = length(fragmentUV);
	//gl_FragColor = vec4(color.rgb, color.a * (pow(0.03, distance) - 0.03));
	gl_FragColor = vec4(color.rgb, color.a * (1.0 - distance));
}